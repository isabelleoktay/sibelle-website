import React from 'react';

const Navigation = (props) => {
    return (
        <nav className='font-bold py-4 px-6 font-playfair flex flex-col text-center sm:flex-row sm:text-left sm:justify-between'>

        </nav>
    );
}

export default Navigation;